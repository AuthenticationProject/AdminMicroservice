# AdminMicroservice
In order to run the application you need a Java environment or Docker installed.
You can run the docker compose to create the services containers for MySQL and the Spring microservice.
```
docker compose up
```
