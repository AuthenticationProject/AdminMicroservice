http://localhost:80 {

    # Spariamo al backend tutto quello che inizia con /api senza toccare una virgola
    handle /api* {
        reverse_proxy adminmicroservice:8090
    }

    # Tutto il resto va al frontend
    handle {
        reverse_proxy frontend:8080
    }
}